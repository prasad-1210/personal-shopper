apiVersion: v2
name: langgraph-primitives
description: >
  LangGraph agent Kubernetes templates.
  Extends k8s-primitives (library) with agent server conventions.
  Deploy one release per agent with layered values (org / env / agent).
type: application
version: 1.0.0
appVersion: "1.0.0"
dependencies:
  - name: k8s-primitives
    version: "1.0.0"
    repository: "file://../k8s-primitives"
